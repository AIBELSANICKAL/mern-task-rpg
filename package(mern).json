{
  "name": "cosmic-rpg-task-manager",
  "version": "1.0.0",
  "description": "Gamified RPG Task Manager with zero-gravity cosmic fantasy theme",
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend --legacy-peer-deps",
    "backend": "npm start --prefix backend",
    "backend:dev": "npm run dev --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "dev": "concurrently \"npm run backend:dev\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
